{% extends 'base.html.twig' %}

{% block title %}Course
{% endblock %}

{% block body %}
	<h1>Course</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Nom</th>
				<td>{{ course.Nom }}</td>
			</tr>
			<tr>
				<th>Date</th>
				<td>{{ course.date }}</td>
			</tr>
			<tr>
				<th>Heure de début</th>
				<td>{{ course.heure }}</td>
			</tr>
			<tr>
				<th>Heure de fin</th>
				<td>{{ course.fin }}</td>
			</tr>
			<tr>
				<th>Niveau</th>
				<td>{{ course.niveau }}</td>
			</tr>
			<tr>
				<th>Public</th>
				<td>{{ course.Public }}</td>
			</tr>
			<tr>
				<th>Membres
				</th>
				<td>
					{% for member in course.members %}
						{{member.nom}}
						{{member.prenom}}
					{% endfor %}
				</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('course_management') }}">back to list</a>

	<a href="{{ path('course_edit', {'id': course.id}) }}">edit</a>

	<a href="{{ path('absence_new_admin') }}">Un membre est absent</a>

	{{ include('course/_delete_form.html.twig') }}
{% endblock %}
